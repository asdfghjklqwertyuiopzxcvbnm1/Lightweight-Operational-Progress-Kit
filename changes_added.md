# LOPK 1.2 添加的功能和改进

## 1. 版本更新

- **版本号**：从 2.0.0 更新到 3.0.0
- **主要改进**：性能优化、功能增强、代码结构优化

## 2. ProgressBar 类

### 新增功能
- **ETA（预计剩余时间）**：显示预计完成时间
- **性能优化**：添加了 0.1 秒的更新间隔，减少终端 IO 操作
- **自动进度限制**：确保进度始终在 0 到 total 之间

### 改进的实现
- **缓存颜色代码**：在初始化时缓存颜色代码，避免重复查找
- **添加更新跟踪**：使用 `last_update` 和 `last_progress` 属性跟踪更新时间和进度
- **优化输出逻辑**：只有当进度变化或超过 0.1 秒时才更新
- **添加 `sys.stdout.flush()`**：确保输出及时显示

### 新增参数
- **show_eta**：控制是否显示预计剩余时间（默认：True）

## 3. Spinner 类

### 改进的实现
- **静态变量**：使用静态变量 `_spinner_chars` 存储旋转字符，避免重复创建
- **缓存消息长度**：在初始化时计算并缓存消息长度，减少重复计算
- **线程超时**：添加了 0.1 秒的线程超时，避免阻塞
- **简化线程创建**：使用 `daemon=True` 参数简化线程创建

## 4. MultiProgressBar 类

### 改进的实现
- **简化初始化**：不再需要预先指定进度条数量 `num_bars`
- **改进的更新逻辑**：清空当前输出并重新打印所有进度条，更可靠
- **简化的光标控制**：使用 `\033[F` 控制光标，避免复杂的光标移动
- **优化的锁机制**：确保多线程安全

## 5. 工具函数

### 新增函数
- **format_file_size**：将字节大小格式化为人类可读格式（如 KB、MB、GB 等）
- **format_time**：将秒数格式化为人类可读格式（如 m s、h m s 等）

### 改进的函数
- **colored_text**：使用全局 `COLORS` 字典，减少重复查找
- **cls**：简化实现，使用单行表达式

## 6. 性能优化

### 终端 IO 优化
- 减少终端输出次数：只有当进度变化或超过 0.1 秒时才更新
- 优化输出内容：减少不必要的字符和格式化

### 内存优化
- 减少对象创建：使用静态变量和全局变量
- 减少内存占用：移除不必要的属性和方法

### 计算优化
- 缓存常用值：如颜色代码、消息长度等
- 减少重复计算：如进度百分比、填充长度等

### 线程优化
- 简化线程管理：使用 `daemon=True` 参数
- 添加线程超时：避免线程阻塞

## 7. 代码结构优化

### 统一颜色管理
- 使用全局 `COLORS` 字典管理所有颜色代码
- 移除重复的颜色定义

### 简化实现逻辑
- 简化条件判断
- 简化函数实现
- 简化类的继承结构

### 提高代码可读性
- 更清晰的命名
- 更简洁的注释
- 更合理的代码组织

## 8. 配置文件更新

### setup.py 更新
- 更新了入口点路径：从 `LOPK11` 改为 `LOPK12`

### pyproject.toml 更新
- 更新了脚本路径：从 `LOPK11` 改为 `LOPK12`
- 更新了 setuptools_scm 配置：从 `LOPK11/_version.py` 改为 `LOPK12/_version.py`

### __init__.py 更新
- 更新了版本号：从 `2.0.0` 改为 `3.0.0`
- 导出了新的工具函数：`format_file_size` 和 `format_time`

## 9. 功能增强

### 跨平台支持
- 改进了跨平台兼容性
- 优化了终端控制字符的使用

### 用户体验改进
- 更准确的进度显示
- 新增的 ETA 功能
- 更流畅的动画效果
- 更清晰的输出信息

## 10. 向后兼容性

- 保持了与 LOPK1.1 的向后兼容性
- 所有现有代码无需修改即可正常工作
- 新增功能为可选参数，默认值保持原有行为

## 11. 代码质量提升

- 减少了代码行数，保持简洁
- 提高了代码的可读性和维护性
- 减少了重复代码
- 优化了错误处理

LOPK1.2 通过这些改进和新增功能，提供了更好的性能、更丰富的功能和更简洁的代码结构，同时保持了与旧版本的向后兼容性。